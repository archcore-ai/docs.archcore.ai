---
// Override Starlight's default Hero component.
// On splash pages we render our own HeroSection inside the MDX content,
// so there's no need for Starlight to also render an <h1>.
// The page <title> from frontmatter still controls the document title and OG/SEO.
---
