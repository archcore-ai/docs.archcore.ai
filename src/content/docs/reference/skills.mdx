---
title: Plugin Skills Reference
description: One-page reference of all 33 Archcore Plugin skills — 9 intent commands, 17 document-type skills, 6 tracks, 1 utility.
---

import PathBadge from '../../../components/PathBadge.astro';

<PathBadge scope="plugin" />

A flat reference of every skill shipped with the Archcore Plugin. For grouped, annotated coverage see [Intent commands](/plugin/intent-commands/), [Tracks](/plugin/tracks/), and [Skills](/plugin/skills/).

## Intent commands (9)

The primary routing commands — describe an intent, the skill picks the right document type.

| Command                 | Purpose                                                                         |
| ----------------------- | ------------------------------------------------------------------------------- |
| `/archcore:capture`     | Document a module, component, or API — routes to adr, spec, doc, or guide.     |
| `/archcore:plan`        | Plan a feature end-to-end — routes to `product-track` or a single plan.        |
| `/archcore:decide`      | Record a finalized decision — creates ADR, offers rule + guide follow-up.      |
| `/archcore:standard`    | Establish a team standard — drives `standard-track` (adr → rule → guide).       |
| `/archcore:review`      | Documentation health review — gaps, staleness, orphans, missing relations.     |
| `/archcore:status`      | Dashboard — counts by category, type, status, relation coverage, tags.         |
| `/archcore:actualize`   | Detect stale docs via code drift, cascade analysis, and temporal staleness.    |
| `/archcore:graph`       | Render the document relation graph as a Mermaid flowchart.                     |
| `/archcore:help`        | Navigate the system — discover skills, types, and workflows.                   |

## Document-type skills (17)

Each skill creates and links one document type. Niche types (mrd, brd, urd, brs, strs, syrs, srs) are hidden from `/` autocomplete and reached programmatically via `sources-track` or `iso-track`. The `plan` document type is created via the `/archcore:plan` intent skill, not a standalone type skill.

### Vision

| Command              | Type   | Visibility | What it captures                                     |
| -------------------- | ------ | ---------- | ---------------------------------------------------- |
| `/archcore:prd`      | prd    | mainstream | Product requirements — goals, scope, success metrics |
| `/archcore:idea`     | idea   | mainstream | Low-commitment concepts and explorations             |
| `/archcore:mrd`      | mrd    | niche      | Market landscape, TAM/SAM/SOM, competition           |
| `/archcore:brd`      | brd    | niche      | Business objectives, stakeholders, ROI               |
| `/archcore:urd`      | urd    | niche      | User personas, journeys, usability requirements      |
| `/archcore:brs`      | brs    | niche      | Formal business requirements spec (ISO 29148)        |
| `/archcore:strs`     | strs   | niche      | Formal stakeholder requirements spec (ISO 29148)     |
| `/archcore:syrs`     | syrs   | niche      | System boundary and interface spec (ISO 29148)       |
| `/archcore:srs`      | srs    | niche      | Software functional/non-functional spec (ISO 29148)  |

### Knowledge

| Command              | Type   | What it captures                                     |
| -------------------- | ------ | ---------------------------------------------------- |
| `/archcore:adr`      | adr    | Architecture decisions with context and consequences |
| `/archcore:rfc`      | rfc    | Proposals open for review and feedback               |
| `/archcore:rule`     | rule   | Mandatory team standards with rationale              |
| `/archcore:guide`    | guide  | Step-by-step how-to instructions                     |
| `/archcore:doc`      | doc    | Reference material — registries, glossaries, lookups |
| `/archcore:spec`     | spec   | Technical contracts for systems and components       |

### Experience

| Command              | Type        | What it captures                                |
| -------------------- | ----------- | ----------------------------------------------- |
| `/archcore:task-type` | task-type  | Recurring task patterns with proven workflows   |
| `/archcore:cpat`     | cpat        | Before/after code pattern changes with scope    |

## Tracks (6)

Multi-step workflows — each orchestrates several documents and wires relations automatically.

| Command                         | Flow                                         | Use when                                                     |
| ------------------------------- | -------------------------------------------- | ------------------------------------------------------------ |
| `/archcore:product-track`       | idea → prd → plan                            | Lightweight product flow — simple and fast.                  |
| `/archcore:sources-track`       | mrd → brd → urd                              | Discovery-focused — market, business, user inputs.           |
| `/archcore:iso-track`           | brs → strs → syrs → srs                      | Formal ISO 29148 cascade with traceability.                  |
| `/archcore:architecture-track`  | adr → spec → plan                            | Design decisions flowing into implementation.                |
| `/archcore:standard-track`      | adr → rule → guide                           | Decision → codified standard → how-to.                       |
| `/archcore:feature-track`       | prd → spec → plan → task-type                | Full feature lifecycle.                                      |

## Utility

| Command               | Purpose                                                   |
| --------------------- | --------------------------------------------------------- |
| `/archcore:verify`    | Plugin integrity check — validates configs, skills, hooks. |

## See also

- [Intent commands](/plugin/intent-commands/) — full descriptions with examples.
- [Tracks](/plugin/tracks/) — flow diagrams and relation wiring per track.
- [Document types](/concepts/document-types/) — required sections per type.
