---
title: Choose Plugin or CLI
description: Two ways to use Archcore — the plugin gives you the highest-level UX in Claude Code and Cursor; the CLI is the minimal direct integration. A short comparison.
---

import PathChooser from '../../../components/PathChooser.astro';

Archcore has two entry points. Both read and write the same `.archcore/` directory, but they meet you at different levels.

## The short answer

- Using **Claude Code or Cursor**? Start with the [Plugin](/plugin/overview/). You get skills, tracks, built-in agents, and guardrails without extra wiring.
- Using **any other agent** (Copilot, Gemini CLI, OpenCode, Codex CLI, Roo Code, Cline), or scripting in CI? Use the [CLI](/cli/overview/) directly.
- Just want to understand the model first? Read the [shared concepts](/concepts/what-is-archcore/).

## Pick your path

<PathChooser />

## How they compare

| | Plugin | CLI |
| --- | --- | --- |
| **Install method** | Plugin marketplace in Claude Code or Cursor | Single binary via curl / PowerShell |
| **Hosts** | Claude Code, Cursor (Copilot + Codex planned) | All 8 supported agents |
| **Interface** | Slash commands (`/archcore:adr`, tracks, reviews) | MCP tools + natural language |
| **Multi-step workflows** | Tracks orchestrate document chains automatically | You drive them manually through MCP |
| **Guardrails** | MCP-only writes, post-mutation validate, cascade warnings | MCP available; guardrails are opt-in via hooks |
| **Best for** | Day-to-day use inside Claude Code or Cursor | Scripting, CI, agents without plugin support |

Both use the same `.archcore/` directory and the same MCP server. Switching paths — or using both at once — requires no migration.

## The mental model

- **CLI is the compiler.** It reads `.archcore/`, builds the graph, speaks MCP.
- **Plugin is the runtime.** It applies that graph inside Claude Code and Cursor — skills, hooks, workflows.

More depth in [Mental Model](/concepts/mental-model/).

## Next steps

- [Plugin quick start](/start/plugin-quick-start/) — install the plugin in Claude Code or Cursor.
- [CLI quick start](/cli/quick-start/) — install the CLI and create your first document.
- [What is Archcore?](/concepts/what-is-archcore/) — the problem, the solution, and the shared mental model.
