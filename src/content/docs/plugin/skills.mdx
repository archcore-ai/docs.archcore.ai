---
title: Skills
description: All 16 Archcore Plugin skills — 9 intent commands, 6 tracks, and 1 utility — invoked via /archcore:* slash commands.
---

import PathBadge from '../../../components/PathBadge.astro';

<PathBadge scope="plugin" />

A **skill** is a unit of expertise the agent loads on demand. Each one carries focused knowledge — when to use a document type, required sections, typical relations, common mistakes — so you describe intent instead of schema. You invoke a skill via a `/archcore:*` slash command, or by simply describing what you want.

The plugin ships **16 skills**: 9 intent commands, 6 tracks, and 1 utility.

There is no per-document-type skill. The 18 [document types](/concepts/document-types/) (`adr`, `prd`, `rule`, `guide`, `spec`, …) are produced by intent commands and tracks, or — when you need a niche type without a routing skill — by calling `mcp__archcore__create_document` from the agent.

## All 16 skills at a glance

| Skill                            | Family   | What it does                                                            |
| -------------------------------- | -------- | ----------------------------------------------------------------------- |
| `/archcore:context`              | intent   | Load rules, ADRs, specs, and patterns that apply to a code area.         |
| `/archcore:bootstrap`            | intent   | First-time onboarding — scale-aware seeding for an empty `.archcore/`. |
| `/archcore:capture`              | intent   | Document a module / component / API — routes to adr, spec, doc, or guide. |
| `/archcore:plan`                 | intent   | Plan a feature — routes to `product-track` or a single plan.            |
| `/archcore:decide`               | intent   | Record a finalized decision — creates ADR, offers rule + guide follow-up. |
| `/archcore:standard`             | intent   | Establish a team standard — drives `standard-track`.                    |
| `/archcore:review`               | intent   | Documentation health — counts dashboard by default, full audit (gaps, staleness, orphans) with `--deep`. |
| `/archcore:actualize`            | intent   | Detect stale docs via code drift, cascade analysis, temporal signals.   |
| `/archcore:help`                 | intent   | List every skill grouped by family.                                     |
| `/archcore:product-track`        | track    | idea → prd → plan.                                                      |
| `/archcore:sources-track`        | track    | mrd → brd → urd.                                                        |
| `/archcore:iso-track`            | track    | brs → strs → syrs → srs (ISO 29148).                                    |
| `/archcore:architecture-track`   | track    | adr → spec → plan.                                                      |
| `/archcore:standard-track`       | track    | adr → (optional cpat) → rule → guide.                                   |
| `/archcore:feature-track`        | track    | prd → spec → plan → task-type.                                          |
| `/archcore:verify`               | utility  | Plugin integrity check — configs, scripts, hooks, test suite.           |

## How skills activate

1. You type a slash command (`/archcore:context`) or describe an intent ("what rules apply to `src/auth/`?").
2. The host matches the request to a skill definition in the plugin package.
3. The skill is loaded into the conversation — its description, guardrails, and relation rules become part of the agent's working context.
4. The agent executes the skill, calling MCP tools as needed.

Every skill ultimately resolves to **MCP tool calls**. The plugin never writes to `.archcore/` directly.

## When to use which family

- **Start with an intent command** when you're not sure which document type fits. `/archcore:capture` and `/archcore:decide` will pick for you.
- **Use a track** when one document alone won't do the job. Need a decision _and_ a rule _and_ a how-to guide? Use `/archcore:standard-track`.
- **Run the utility** (`/archcore:verify`) when you suspect a plugin install issue — it checks configs, scripts, skills, hooks, and runs the test suite.

## Intent commands

Intent commands are the primary user interface of the plugin. You describe what you want, the command routes to the right document type and the right multi-step flow.

Use these when you don't want to think about which of the 18 document types fits — the command figures it out.

### `/archcore:context`

"Before I touch this code, what's already decided?"

```
/archcore:context src/auth/
/archcore:context payment retries
```

Loads the rules, ADRs, specs, and patterns tied to that path or topic — grouped by type, ranked by specificity — so the agent edits inside the lines your team has already drawn. Also useful for picking up work: `/archcore:context where am I on the auth redesign?`.

The same context is _also_ injected automatically before any source-file edit (see [How it works](/plugin/how-it-works/)). Run the command explicitly when you want to see the full set, not just the top 3.

### `/archcore:bootstrap`

"Empty repo. What goes in `.archcore/` first?"

```
/archcore:bootstrap
```

Detects repo scale (small / medium / large) and seeds scale-appropriate documents: a stack rule, a run-the-app guide, an entry-point inventory (medium+), a top-level domain map (large), and a list of hotspot capture candidates.

If you already have agent-instruction files — `CLAUDE.md`, `AGENTS.md`, `.cursorrules` — bootstrap also offers to import them, so you don't lose existing context.

Run this once per project. After that, `/archcore:capture` and `/archcore:decide` take over.

### `/archcore:capture`

"I have this thing in the codebase and it isn't documented yet."

```
/archcore:capture webhook delivery pipeline
```

The skill picks: `adr` if it's a decision, `spec` if it's a contract, `doc` if it's reference material, `guide` if it's a how-to. You approve before anything is written.

### `/archcore:plan`

"We're about to build something and I want it scoped."

```
/archcore:plan auth redesign
```

Small ask: single `plan` document. Bigger ask: routes to `product-track` (idea → prd → plan).

### `/archcore:decide`

"We've decided X, record it."

```
/archcore:decide use PostgreSQL as the primary database
```

Creates an ADR. Offers a rule + guide follow-up when the decision implies a standard.

### `/archcore:standard`

"This is going to be mandatory across the team."

```
/archcore:standard migration format
```

Triggers the [standard-track](#standard-track): `adr` (decision) → `rule` (standard) → `guide` (how to follow it). All relations wired automatically.

### `/archcore:review`

"Show me where we are." or "Audit the docs."

```
/archcore:review                  # default short mode — counts dashboard
/archcore:review --deep           # full audit, project-wide
/archcore:review --deep tag:auth  # full audit, scoped to a tag
/archcore:review knowledge        # any non-flag filter implies --deep
```

Default mode returns a one-screen dashboard: counts by category, status, type, relation coverage, and a one-line issues summary. Add `--deep` (or any filter) to switch to the full audit — gaps, staleness, orphans, missing relations, and prioritized recommendations.

The dashboard is project-wide by design and does not take filters. Pass a filter when you want analysis of that scope.

### `/archcore:actualize`

"What's out of date?"

```
/archcore:actualize
/archcore:actualize auth/
```

Looks for staleness signals: code drift (document references source that changed), cascade drift (a dependency updated without the dependent being reviewed), and temporal drift (a document hasn't been touched in a long time).

### `/archcore:help`

"What commands are even available?"

```
/archcore:help
```

Lists every skill grouped by family (intent commands, tracks, utility) with one-line descriptions.

## Tracks

A **track** is a multi-step workflow that orchestrates several documents at once. Each step builds on the previous one, and relations between documents are created automatically so the graph stays consistent.

Use a track when one document is not enough — when a decision needs a rule and a guide, when a feature needs discovery and a plan, when a regulated system needs the full ISO 29148 cascade.

### Track flows

| Track                | Flow                                         | Use when                                               |
| -------------------- | -------------------------------------------- | ------------------------------------------------------ |
| `product-track`      | idea → prd → plan                            | Lightweight product flow — simple and fast.            |
| `sources-track`      | mrd → brd → urd                              | Discovery-focused — market, business, then user inputs. |
| `iso-track`          | brs → strs → syrs → srs                      | Formal ISO 29148 cascade with traceability.            |
| `architecture-track` | adr → spec → plan                            | Design decisions flowing into implementation.          |
| `standard-track`     | adr → rule → guide¹                          | Decision → codified standard → how-to.                 |
| `feature-track`      | prd → spec → plan → task-type                | Full feature lifecycle.                                |

¹ `standard-track` accepts an optional `cpat` step between `adr` and `rule` when the standard is best taught by example. See the [Standard track](#standard-track) section below.

Invoke any track:

```
/archcore:product-track auth redesign
/archcore:architecture-track event-driven orders
/archcore:iso-track payment gateway compliance
```

### How a track runs

1. The skill asks a short framing question (scope, constraints, stakeholders).
2. It creates the first document through MCP.
3. It creates each subsequent document, passing context forward, and wires `implements` / `depends_on` / `related` relations automatically.
4. You review each step — no document is final until you say so.

You can stop at any step. A `product-track` that only needs an idea and a PRD simply ends after step two.

### Product track

Lightweight flow for individual features or small teams.

**Flow:** `idea → prd → plan`
**Relations created:** `prd implements idea`, `plan implements prd`.
**Use when:** you don't need formal requirements decomposition — just capture, scope, and go.

### Sources track

Discovery-focused flow for product teams doing research and stakeholder alignment.

**Flow:** `mrd → brd → urd`
**Relations created:** `brd related mrd`, `urd related mrd, brd`.
**Use when:** you need to understand the market, business case, and user context before writing requirements.

### ISO track

Formal ISO 29148 cascade.

**Flow:** `brs → strs → syrs → srs`
**Relations created:** `strs implements brs, urd`; `syrs implements strs`; `srs implements syrs`.
**Use when:** regulated systems, multi-team projects, or any time you need traceability from business intent down to software requirements.

### Architecture track

Design decisions flowing into implementation.

**Flow:** `adr → spec → plan`
**Relations created:** `spec implements adr`, `plan implements spec`.
**Use when:** a significant technical decision needs formal specification and an implementation plan. No rule/guide layer — that's what standard-track is for.

### Standard track

Decision codified as a mandatory practice.

**Flow:** `adr → (optional cpat) → rule → guide`
**Relations created:** `rule implements adr`, `guide related rule`. When a `cpat` is added, it captures the before/after pattern, with `cpat related rule` wiring it to the standard.
**Use when:** the decision implies a team standard that needs enforcement and instructions. The classic chain for "we decided X, now everyone needs to follow it". Add a `cpat` step when the standard is best taught by example (e.g., "old way vs. new way").

### Feature track

Full feature lifecycle with repeatable delivery pattern.

**Flow:** `prd → spec → plan → task-type`
**Relations created:** `spec implements prd`, `plan implements spec`, `task-type related plan`.
**Use when:** a well-scoped feature needs formal specification and a delivery pattern you can apply to similar features later.

### Product-track vs sources-track vs ISO-track

All three start in the Vision layer, but they answer different questions:

- **product-track** — "what are we building?" (fast, informal)
- **sources-track** — "what do we know about the problem space?" (discovery)
- **iso-track** — "how is this formally specified and traceable?" (compliance)

You can run them in sequence (`sources-track` → `product-track` → `architecture-track`), or only the ones you need.

### Architecture-track vs standard-track

Both start from an ADR.

- **architecture-track** — the decision leads to a formal spec and an implementation plan.
- **standard-track** — the decision is codified as a rule and explained in a guide.

Often a single ADR benefits from both.

## Discovering skills at runtime

Inside any plugin-enabled session:

```
/archcore:help
```

Returns the same list grouped by family, with one-line descriptions. Useful when you forget the exact command name.

## Next steps

- [Built-in agents](/plugin/agents/) — the two agents that back skills for complex flows.
- [Document types](/concepts/document-types/) — the 18 types intent commands and tracks route to.
- [Track reference](/reference/tracks/) — one-page summary of all 6 tracks.
